"""Bespoke resource rendering (currently a stub).

All resource envelopes have a column spec and a ``rows`` list, so the
generic :class:`RichTableExporter` handles every report adequately.
This module exists as a placeholder for any future
resource-specific renderer (e.g. nested tables for pie reports).
"""
