from .common_imports import *


def add_directories_indexing():
    with create_progress_bar(
    ) as progress:
        task = progress.add_task(
            f"[green]Indexing directories table...",
        )
        pass

    return



def add_directory_stats_indexing():
    return
